\documentclass[14pt,a4paper]{extarticle}
\usepackage[unicode,colorlinks=true]{hyperref}
\usepackage[czech]{babel}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{graphicx}
\usepackage{setspace}
\textwidth 18cm \textheight 26.6cm
\topmargin -2.5cm
\oddsidemargin -1cm

% ALGORITHM typesetting
\input{../../settings/algorithm_settings_normal}

% Xpatch upavuje sirku sadzby algoritmov
\input{../../settings/xpatch_settings}
\AtBeginEnvironment{procedure}{\setstretch{1.3}}

\begin{document}

\pagestyle{empty}
\noindent
\setlength\parindent{0pt}

\begin{procedure}[H]
	\caption{Enqueue($\mathit{queue, key}$)}
	\Input{struktura $\mathit{queue}$, klíč $\mathit{key}$}
	\BlankLine
	$\mathit{added} \leftarrow \ffnew~ Node(\mathit{key})$ \;
	$\mathit{added.left} \leftarrow$ \;
	\eIf{$\hspace{3cm} = \mathit{nil}$} { }{ }
	$\mathit{queue.last} \leftarrow \mathit{added}$ \;
\end{procedure}

\vspace{1cm}

\begin{procedure}[H]
	\caption{Dequeue($\mathit{queue}$)}
	\Input{struktura $\mathit{queue}$}
	\Output{hodnota odstraněného prvku}
	\BlankLine
	\If{$\hspace{6cm}$}{
		\Return{fronta je prázdná}
	}
	  \;
	$\mathit{tmp} \leftarrow \mathit{queue.first}$ \tcp{abychom neztratili ukazatele na prvek}
	\eIf{$\hspace{5cm}$}{
		  \;
		  \;
	} {
		$\mathit{queue.first} \leftarrow \mathit{queue.first.left}$ \;
	}
	  \;
	\Return{$\mathit{key}$} \;
\end{procedure}

\end{document}
