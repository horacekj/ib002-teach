\documentclass[14pt,a4paper]{extarticle}
\usepackage[unicode,colorlinks=true]{hyperref}
\usepackage[czech]{babel}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{graphicx}
\usepackage{setspace}
\textwidth 18cm \textheight 26.6cm
\topmargin -2.7cm
\oddsidemargin -1cm

% ALGORITHM typesetting
\input{../../settings/algorithm_settings_normal}

% Xpatch upavuje sirku sadzby algoritmov
\input{../../settings/xpatch_settings}
\AtBeginEnvironment{procedure}{\setstretch{1.3}}

\begin{document}
\pagestyle{empty}
\noindent

\begin{procedure}
\LinesNumberedHidden
\setstretch{1.5}
	\caption{LeftRotate($tree,x$)}
	\Input{strom $tree$ a jeho uzel $x$, který je kořenem rotace}
	\BlankLine
	$y \leftarrow \mathit{x.right}$ \;
	$\mathit{x.right} \leftarrow$ \;
	\If{$\mathit{y.left} \neq \mathit{nil}$}{
		$\mathit{y.left.parent} \leftarrow$
	}
	$\mathit{y.parent} \leftarrow$ \;
	\uIf{$\hspace{4cm}$}{
		$\mathit{tree.root} \leftarrow y$
	}\uElseIf{$x = \mathit{x.parent.left}$}{}\Else{}
$\mathit{y.left} \leftarrow x$ \;
$\mathit{x.parent} \leftarrow y$\;
\end{procedure}

\end{document}
