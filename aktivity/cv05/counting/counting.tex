\documentclass[20pt,a4paper,landscape]{extarticle}
\usepackage[unicode,colorlinks=true]{hyperref}
\usepackage[czech]{babel}
\usepackage[utf8]{inputenc}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{graphicx}
\usepackage{setspace}
\textwidth 26.6cm \textheight 16cm
\topmargin -2.5cm
\oddsidemargin -1cm

% ALGORITHM typesetting
\input{../../settings/algorithm_settings_normal}

% Xpatch upavuje sirku sadzby algoritmov
\input{../../settings/xpatch_settings}

\begin{document}
\pagestyle{empty}
\noindent

\begin{function}[H]
	\caption{CountingSort($A,n,k$)}
	\Input{pole $A$ délky $n$ s~hodnotami z~intervalu $\langle 1,\dots,k \rangle$}
	\Output{vzestupně seřazené pole $A$ původních prvků}
	\BlankLine

	\For{$i \leftarrow 2$ \KwTo $k$}{}
	\For{$i \leftarrow 1$ \KwTo $k$}{}
	\For{$i \leftarrow n$ \KwDownTo $1$}{}
	\For{$i \leftarrow 1$ \KwTo $n$}{}

	$\mathit{count}[A[i]] \leftarrow \mathit{count}[A[i]] + 1$\;
	$B[\mathit{count}[A[i]]] \leftarrow A[i]$ \;
	$\mathit{count}[A[i]] \leftarrow \mathit{count}[A[i]] - 1$ \;
	$\mathit{count}[i] \leftarrow \mathit{count}[i] + \mathit{count}[i - 1]$ \;
	$\mathit{count}[i] \leftarrow 0$ \;

	\Return{$B$}
\end{function}

\end{document}
